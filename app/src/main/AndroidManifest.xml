<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smart.teacher.attendance.vasanth.smartattendanceforteacher">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Year">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Splash" />
        <activity android:name=".Subjects" />
        <activity android:name=".Sections" />
        <activity android:name=".Branch" />
        <activity android:name=".Students" />
        <activity android:name=".AttendanceInTeacher" />
        <activity android:name=".RegisteredStudents" />
        <activity android:name=".CheckAttendanceOfStudents" />
        <activity android:name=".ManualAttendance" />
        <activity android:name=".OverallAttendance">

        </activity>
    </application>

</manifest>